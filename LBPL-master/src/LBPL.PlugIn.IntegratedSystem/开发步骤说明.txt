1、新建一个 末尾以 ServicePlugin的类库

2、新增CronConfig、对应的Job、JobLibray、JobsConfig 这几个文件，名称不能修改对应的

3、在主程序中App.Config AppSetting 中加入对应的时间表达式（后期拓展：自定义解析xml）

4、主程序采用反射机制，动态注入插件，并且让服务初步生成


5、新增的插件 先手动加到主程序对应的文件夹，后续 在插件类库中直接编译生成过来，如果是任务调用系统，直接采用文件上传解析zip，里面有对应的配置项
（在类库引用 中选择重复的无关的 复制到本地 设置 fasle，并且在类库属性 生成中 设置 生成位置为：..\LBPL.PlugIn.IntegratedSystem\bin\Debug\ServicePlugins\）

后续展开zip包配置项


